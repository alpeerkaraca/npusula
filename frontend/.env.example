# Frontend transport: mock uses sample data; http never falls back to mock.
VITE_API_MODE=mock
VITE_API_BASE_URL=/api
# Development-only proxy to your backend (do not prefix secrets with VITE_).
API_PROXY_TARGET=http://127.0.0.1:8000
